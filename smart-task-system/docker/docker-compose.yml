version: '3'
services:
  app:
    build: ..
    ports:
      - "3000:3000"
    depends_on:
      - mongo
      - mysql

  mongo:
    image: mongo

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: task_logs

      